var config = {
    apiUrl: "http://meals-on-wheels.herokuapp.com/api/", //API URL for meals on wheels software.
    apiVersion: "v1",
    requestDate: "current" //Set to date with "year-month-day" format if you want tours for a specific date.
}
